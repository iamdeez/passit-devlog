apiVersion: v1
kind: Service
metadata:
  name: ticketservice-service
  labels:
    app: ticketservice-service
spec:
  type: ClusterIP
  selector:
    app: ticketservice-service
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
