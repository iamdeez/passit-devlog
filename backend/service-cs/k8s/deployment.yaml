apiVersion: apps/v1
kind: Deployment
metadata:
  name: cs-service
  labels:
    app: cs-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cs-service
  template:
    metadata:
      labels:
        app: cs-service
    spec:
      containers:
      - name: cs-service
        image: cs-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: DB_HOST
          valueFrom:
            configMapKeyRef:
              name: cs-config
              key: db.host
        - name: DB_PORT
          valueFrom:
            configMapKeyRef:
              name: cs-config
              key: db.port
        - name: DB_NAME
          valueFrom:
            configMapKeyRef:
              name: cs-config
              key: db.name
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: cs-secret
              key: db.user
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: cs-secret
              key: db.password
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 5
