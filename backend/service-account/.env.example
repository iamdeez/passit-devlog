# Database Configuration
DB_HOST=localhost
DB_PORT=3307
DB_NAME=passit_db
DB_USER=passit_user
DB_PASSWORD=passit_password

# Application Configuration
SPRING_PROFILES_ACTIVE=default  # 'default' = 콘솔 로그, 'smtp' = 실제 이메일 전송
SERVER_PORT=8081

# Email Configuration (SMTP 프로필 사용 시 필요)
# Gmail 사용 시 앱 비밀번호 발급 필요: https://myaccount.google.com/apppasswords
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-16-digit-app-password
